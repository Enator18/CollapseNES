MEMORY {
    #ZEROPAGE
    ZP:     file = "", start = $0000, size = $0100, type = rw, define = yes;

    # INES Cartridge Header
    ROMHEADER: file = %O, start = $0000, size = $0010, fill = yes;

    #PRG ROM
    PRG:   file = %O, start = $8000, size = $8000, fill = yes, define = yes, bank = $00;

    #CHR ROM
    CHR:   file = %O, start = $0000, size = $2000, fill = yes;

    #SRAM
    SRAM:   file = "", start = $0400, size = $0400, define = yes;
}
SEGMENTS {
    ZEROPAGE: load = ZP,              type = zp;
    HEADER:   load = ROMHEADER,          type = ro;
    CODE:     load = PRG,       type = ro,  define   = yes;
    VECTORS:  load = PRG,       type = ro,  start    = $FFFA;
    CHARS:    load = CHR,             type = ro;
    RAM:     load = SRAM,         type = rw,  define   = yes;
}